// import { Doctor } from "../../models/doctor/doctor.model.ts";
const NexmoService = {
  requestOtp: async (telephone: string) => {
    return Promise.resolve(true)
  },
};

export default NexmoService;
